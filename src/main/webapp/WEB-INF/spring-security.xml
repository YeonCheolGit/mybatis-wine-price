<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd
             http://www.springframework.org/schema/security">

    <beans:bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <beans:bean id="userDetailsService" class="org.springframework.security.core.userdetails.jdbc.JdbcDaoImpl">
        <beans:property name="dataSource" ref="dataSource"/>
    </beans:bean>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service
                    data-source-ref="dataSource"
                    users-by-username-query="select email, pwd, nickName from member where email = ?"
                    authorities-by-username-query="select authority from authorities where email = ?"
            />
        </authentication-provider>
    </authentication-manager>

    <http use-expressions="true">
        <intercept-url pattern="/main" access="permitAll" />
        <intercept-url pattern="/member/**" access="permitAll" />
<!--        <intercept-url pattern="/wine/**" access="hasRole('USER')" />-->
        <csrf/>

        <form-login
                login-page="/main"
                default-target-url="/main"
                username-parameter="login_email"
                password-parameter="login_pwd"
        />
    </http>

</beans:beans>